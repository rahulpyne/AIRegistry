# Copy to .env for local dev (App Service uses application settings instead).

# Cosmos DB for MongoDB connection string (Azure Portal > Cosmos account >
# Connection strings > PRIMARY CONNECTION STRING). Treat as a secret.
COSMOS_MONGO_CONNECTION_STRING=
COSMOS_DB_NAME=airegistry
COSMOS_COLLECTION=registry_entries

# Local port (App Service sets PORT automatically).
PORT=8080
